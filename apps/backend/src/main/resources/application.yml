spring:
  profiles:
    active: dev
  application:
    name: postsales-backend
  liquibase:
    change-log: classpath:db/changelog/db.changelog-master.yaml
    enabled: true
  jackson:
    serialization:
      write-dates-as-timestamps: false

server:
  address: 0.0.0.0
  port: ${PORT:8080}

# Force Railway redeploy 2

